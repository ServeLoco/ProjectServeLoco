import React from 'react';
import AdminPlaceholderScreen from './AdminPlaceholderScreen';

// Filled in by ADMIN TASK 14 — live presence, summary cards, top lists, search.
export default function AdminAnalyticsScreen() {
  return <AdminPlaceholderScreen title="Live" subtitle="Live analytics land here next." />;
}
