import React from 'react';
import AdminPlaceholderScreen from './AdminPlaceholderScreen';

// Filled in by ADMIN TASK 9 — list/detail, status/payment updates, live popup, push deep-link.
export default function AdminOrdersScreen() {
  return <AdminPlaceholderScreen title="Orders" subtitle="Order list and live alerts land here next." />;
}
