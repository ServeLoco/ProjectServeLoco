import React from 'react';
import AdminPlaceholderScreen from './AdminPlaceholderScreen';

// Filled in by ADMIN TASK 13 — broadcast composer, history, event templates.
export default function AdminNotificationsScreen() {
  return <AdminPlaceholderScreen title="Alerts" subtitle="Broadcasts and templates land here next." />;
}
