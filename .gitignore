node_modules
.expo
dist
build
.env
.env.local
.env.*
!.env.example
!.env.*.example
*.log
coverage
firebase-service-account.json
*-firebase-adminsdk-*.json
# google-services.json is committed — it contains only public config
# (API key, project ID) and is required by the CI/CD build pipeline.
GoogleService-Info.plist
.antigravitycli/
