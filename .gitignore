node_modules
.expo
dist
build
.env
.env.local
.env.*
!.env.example
!.env.*.example
*.log
coverage
firebase-service-account.json
*-firebase-adminsdk-*.json
# google-services.json and GoogleService-Info.plist are committed — they hold
# only public client config (API key, project ID) that ships inside every
# released APK/IPA anyway, and both are required by the CI/CD build pipeline.
.antigravitycli/

# AI tool configs/state
.claude/
.codebase-memory/
.kimchi/
.opencode/
.mcp.json
skills-lock.json

# Written on the production box by the deploy workflow (last good image tag)
.deploy-tag
