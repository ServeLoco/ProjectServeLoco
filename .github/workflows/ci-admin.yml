name: Admin Manager CI

on:
  push:
    branches: [ "main" ]
    paths:
      - 'adminManager-V1/**'
  pull_request:
    branches: [ "main" ]
    paths:
      - 'adminManager-V1/**'

jobs:
  test-and-lint:
    runs-on: ubuntu-latest
    
    defaults:
      run:
        working-directory: ./adminManager-V1

    steps:
    - name: Checkout code
      uses: actions/checkout@v4

    - name: Setup Node.js 20
      uses: actions/setup-node@v4
      with:
        node-version: '20'
        cache: 'npm'
        cache-dependency-path: './adminManager-V1/package-lock.json'

    - name: Install dependencies
      run: npm ci

    - name: Run Linter
      run: npm run lint
      
    - name: Build project
      run: npm run build
